ClientVault is a web-based customer management system built with ASP.NET MVC.
It demonstrates core backend concepts such as CRUD operations, ADO.NET data access, model validation, and clean separation of concerns.
